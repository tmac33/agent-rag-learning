# Copy to .env and fill in real values. Never commit .env.

# LLM providers
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Vector DB (if hosted)
PINECONE_API_KEY=
CHROMA_HOST=localhost
CHROMA_PORT=8000

# Optional: tracing / eval
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=agent-rag-learning
